#############
Notes on the issues
Known issues: Snubbull enemy anim0 and anim1
The datafiles that need to be in lists are the ones that are in order, so the ordering is not an issue
have already ran a make clean 
Tried porting over the clean version of snubbull assets from polished crystal to see if that's glitchy. The result: 
I also looked at whether the dimensions are somehow getting corrupted somewhere else. Marill, a known bad, has a dimension file of U (40?) which matches the dimension file of Para (40?) which is OK. 

One possible issue is that certain banks are overflowing and that's why the graphics are bad? gfx/pics.asm? I should double check that this file is properly ordered. 

There are two big clusters of bad mons mostly sudowdoodo to teddiursa.

Part of the problem is that shieldon was calling frames it didn't have. It should have had empty anim0 anim1. That's an easy issue to fix, and when I delete the animation lines, then the result is that shieldon is OK now. The problem with nearby cranidos is probably due to a bad anim file, so I commented those lines out. 

I noticed that the scythe file is really big. So I commented out the anim0. The result is that sudowoodo is still bad. I also commented out the kleavor animation since that's big and still get problems with sudowoodo. 

Everything else in the section should be OK. So I'm not sure what I can do other than run a make clean and see what happens. Make clean still shows a problem with sudowoodo, even though all the other members of its section seem OK. 

Important finding is that the above sudowoodo to ursaluna error is a problem that terminates at the end of kanto frames and once johto frames starts there is no problem again, but it's possible that some mons animated forms are also bad there


Notes:
1. Pikachu, Exeggcute, and Cubone are all able to evolve into their form change by giving evolution stone to evolve into another pokemon with the odd souvenir. I should check if this is still true in my case that they will evolve into araichu, etc. 
----> Result: These pokemon do not form change with odd souvenir evolution. 


C:\Users\dwg11\PLC_Polished\engine\pokemon\evolve.asm:
  Line 427: cp ODD_SOUVENIR ; HOPEFULLY THIS ITEM IS JUST NOT AVAILABLE?

#################Clean

C:\Users\dwg11\polishedcrystal_clean\data\pokemon\evos_attacks.asm:
  Line 446: db EVOLVE_ITEM, ODD_SOUVENIR, RAICHU
  Line 2289: db EVOLVE_ITEM, ODD_SOUVENIR, EXEGGUTOR
  Line 2334: db EVOLVE_ITEM, ODD_SOUVENIR, MAROWAK

C:\Users\dwg11\polishedcrystal_clean\engine\items\item_effects.asm:
  Line 236: dw EvoStoneEffect     ; ODD_SOUVENIR

C:\Users\dwg11\polishedcrystal_clean\engine\pokemon\evolve.asm:
  Line 427: cp ODD_SOUVENIR